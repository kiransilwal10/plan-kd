seed: 42
data_cfg: configs/data.yaml
model_cfg: configs/model.yaml
output_root: outputs
stage: evidence_kd
trainer:
  epochs: 1
  batch_size: 4
  grad_accum: 4
  num_workers: 2
  log_interval: 10
  save_every: 1000
  device: cuda
loss:
  ce_weight: 1.0
  kd_weight: 1.0
  evidence_weight: 1.0
  temperature: 2.0
